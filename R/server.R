require(transportr)
require(dplyr)
require(tidyr)
require(ggplot2)
require(ggmap)
require(sp)
require(rgdal)
require(leaflet)
require(stplanr)
require(readr)
require(rgeos)
require(maptools)
require(iterators)

dashboardPage(
  dashboardHeader(title = "CitiBike Data Explorer"),
  dashboardSidebar(



  )
)
